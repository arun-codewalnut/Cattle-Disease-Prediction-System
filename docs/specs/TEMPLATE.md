# Spec: <short title>

**Milestone**: <M1–M8 or "stretch">
**Status**: draft | agreed | in progress | done

## Actor + goal

<Who is doing this, and what state change are they trying to cause?>

## Boundaries & failure states

<What's explicitly out of bounds? What happens on invalid input, timeout, partial failure?>

## Examples

<Concrete sample inputs → expected outputs. Include at least one happy path and one edge
case. For an API: sample request/response JSON. For UI: sample states/screens.>

## Not in scope

<Explicitly list what this spec does NOT cover, so the agent stops guessing at boundaries.>

## Acceptance criteria (must be checkable)

- [ ] <criterion 1 — phrased so it's objectively true/false>
- [ ] <criterion 2>

## Agent mirror-back (fill before coding starts)

<Before writing code: restate the intent, list inputs/outputs, and flag anything you had to
assume. This catches misunderstandings before the first diff, not in review.>
