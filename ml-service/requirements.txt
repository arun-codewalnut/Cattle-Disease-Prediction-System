fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-dotenv==1.0.1
httpx==0.28.1

# ML
xgboost==2.1.3
scikit-learn==1.6.0
pandas==2.2.3

# Image classifier (M9) — CPU wheels. The +cpu version suffix + extra-index-url below make
# `pip install -r requirements.txt` alone resolve the CPU build, same as CI does — a bare
# `torch==2.14.0` (no +cpu) silently resolves to PyPI's default CUDA build instead, which
# pulls in hundreds of MB of nvidia-*/triton/cuda-toolkit packages and blew past this repo's
# CI runner's disk quota the first time this was pushed (confirmed via the failed run's log,
# not guessed).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.14.0+cpu
torchvision==0.29.0+cpu

# Agent / RAG
# No vector store: retrieval reads the markdown reference docs directly — see
# docs/specs/remove-databases.md. That also removed chroma-hnswlib, which had no cp313
# wheel and was the reason this service's tests had to run in Docker.
langgraph==0.2.60
langchain-core==0.3.28
langchain-ollama==0.2.2

# Dev/test
pytest==8.3.4
