# Chroma runs embedded (chromadb.PersistentClient, app/rag/retrieval.py) as of M6, not as
# a networked server — CHROMA_HOST/CHROMA_PORT are unused. See docs/DECISIONS.md.
LLM_PROVIDER=ollama
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.1
