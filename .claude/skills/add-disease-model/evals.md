# Evals: add-disease-model

## Should trigger

- "add a model for mastitis detection"
- "train a new classifier for lumpy skin disease"
- "wire up a new prediction capability in the agent"

## Should NOT trigger

- "fix a bug in the existing XGBoost model" (modifying, not adding — playbook's steps 1-2
  don't apply; this should just be a normal edit)
- "explain how the current model works" (informational)
